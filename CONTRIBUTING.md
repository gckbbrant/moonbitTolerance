# Contributing

Run `moon fmt --check`, `moon check --deny-warn`, `moon test --target wasm-gc --deny-warn`, and `moon info` before opening a change. Keep public types in the root package, add black-box tests for new behavior, and update the README or roadmap when the supported scope changes.
